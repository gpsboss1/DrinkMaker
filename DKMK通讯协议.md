# STM32 ↔ ESP32 串口通信协议文档

**版本：V1.0**
**通信方式：UART**
**适用系统：固体饮料自动冲调设备**

---

# 1. 系统架构

本系统采用 **双MCU架构**：

| 设备    | 职责                          |
| ----- | --------------------------- |
| ESP32 | UI界面、触摸屏交互、WiFi通信、AI控制、参数设置 |
| STM32 | 实时控制（称重、水泵、加热、料仓电机等）        |

通信方式：

```
ESP32  ←UART→  STM32
```

ESP32 发送控制指令
STM32 执行并返回状态。

---

# 2. 通讯参数

| 参数  | 值      |
| --- | ------ |
| 波特率 | 115200 |
| 数据位 | 8      |
| 停止位 | 1      |
| 校验位 | None   |

---

# 3. 通讯帧结构

所有通信均使用统一帧格式：

| 字段   | 字节 | 说明   |
| ---- | -- | ---- |
| HEAD | 2  | 帧头   |
| DEV  | 1  | 设备ID |
| CMD  | 1  | 命令字  |
| LEN  | 1  | 数据长度 |
| DATA | N  | 数据   |
| CRC  | 1  | 校验   |

完整格式：

```
HEAD1 HEAD2 DEV CMD LEN DATA CRC
```

---

# 4. 帧头定义

| 字节    | 值    |
| ----- | ---- |
| HEAD1 | 0xAA |
| HEAD2 | 0x55 |

---

# 5. 设备ID

| ID   | 设备       |
| ---- | -------- |
| 0x01 | STM32控制器 |
| 0x02 | ESP32控制器 |

---

# 6. CRC校验

CRC采用 **简单累加校验**：

```
CRC = DEV + CMD + LEN + DATA (逐字节累加)
```

只保留 **低8位**。

示例：

```
DEV  = 01
CMD  = 11
LEN  = 02
DATA = 00 5A

CRC = 01 + 11 + 02 + 00 + 5A
CRC = 6E
```

完整数据：

```
AA 55 01 11 02 00 5A 6E
```

---

# 7. 命令字定义

## 7.1 系统控制命令

| CMD  | 功能   | 方向          |
| ---- | ---- | ----------- |
| 0x01 | 心跳   | 双向          |
| 0x02 | 查询状态 | ESP32→STM32 |
| 0x03 | 紧急停止 | ESP32→STM32 |

---

## 7.2 参数设置命令

| CMD  | 功能     | DATA |
| ---- | ------ | ---- |
| 0x10 | 设置饮品类型 | 1字节  |
| 0x11 | 设置水温   | 2字节  |
| 0x12 | 设置水量   | 2字节  |
| 0x13 | 设置粉末重量 | 2字节  |

---

### 设置饮品类型

DATA：

| 值    | 含义  |
| ---- | --- |
| 0x01 | 饮品1 |
| 0x02 | 饮品2 |

示例：

```
AA 55 01 10 01 01 CRC
```

---

### 设置水温

单位：摄氏度

DATA：

```
uint16_t temperature
```

示例：

设置 **90°C**

```
AA 55 01 11 02 00 5A CRC
```

---

### 设置水量

单位：ml

DATA：

```
uint16_t volume
```

示例：

设置 **200ml**

```
AA 55 01 12 02 00 C8 CRC
```

---

### 设置粉末重量

单位：g

DATA：

```
uint16_t weight
```

示例：

设置 **50g**

```
AA 55 01 13 02 00 32 CRC
```

---

# 8. 执行命令

| CMD  | 功能   |
| ---- | ---- |
| 0x20 | 开始冲调 |
| 0x21 | 取消冲调 |

示例：

开始冲调：

```
AA 55 01 20 00 CRC
```

---

# 9. STM32状态返回

STM32主动向ESP32上报状态。

| CMD  | 功能   |
| ---- | ---- |
| 0x80 | 状态上报 |
| 0x81 | 错误上报 |
| 0x82 | 冲调完成 |

---

## 状态上报

DATA：

| 状态值  | 含义   |
| ---- | ---- |
| 0x00 | 空闲   |
| 0x01 | 等待杯子 |
| 0x02 | 投放粉末 |
| 0x03 | 加热中  |
| 0x04 | 出水中  |
| 0x05 | 完成   |

示例：

```
AA 55 01 80 01 03 CRC
```

表示：

```
当前状态：加热中
```

---

# 10. 错误上报

| 错误码  | 含义     |
| ---- | ------ |
| 0x01 | 未检测到杯子 |
| 0x02 | 称重异常   |
| 0x03 | 水温异常   |
| 0x04 | 出水超时   |

示例：

```
AA 55 01 81 01 01 CRC
```

表示：

```
错误：未检测到杯子
```

---

# 11. 冲调完成

STM32完成后发送：

```
AA 55 01 82 00 CRC
```

ESP32收到后更新UI状态。

---

# 12. 冲调完整流程

用户操作流程：

```
选择饮品
    ↓
选择温度
    ↓
选择水量
    ↓
选择粉末量
    ↓
点击开始
```

ESP32发送：

```
设置饮品
设置温度
设置水量
设置粉末
开始冲调
```

STM32执行：

```
等待杯子
    ↓
投放粉末
    ↓
加热水
    ↓
出水
    ↓
完成
```

STM32最终返回：

```
冲调完成
```

---

# 13. 示例完整通信

设置参数并开始冲调：

```
AA 55 01 10 01 01 CRC   // 饮品1
AA 55 01 11 02 00 5A CRC // 90℃
AA 55 01 12 02 00 C8 CRC // 200ml
AA 55 01 13 02 00 32 CRC // 50g
AA 55 01 20 00 CRC       // 开始冲调
```

STM32返回：

```
AA 55 01 80 01 02 CRC   // 投粉
AA 55 01 80 01 03 CRC   // 加热
AA 55 01 80 01 04 CRC   // 出水
AA 55 01 82 00 CRC      // 完成
```

---

# 14. 未来扩展

协议预留扩展空间：

可增加：

* 水位检测
* 清洗模式
* 自动除垢
* 远程升级
* AI控制

新增命令建议使用：

```
0x30 - 0x3F
```

作为扩展命令区。

---

# 15. 版本历史

| 版本   | 日期   | 内容   |
| ---- | ---- | ---- |
| V1.0 | 2026 | 初始版本 |

---
