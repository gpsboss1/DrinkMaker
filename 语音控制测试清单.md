# 小饮语音控制功能测试清单

## 测试说明
- **期望回复**：AI应该说出的话（不超过两句，干练有梗）
- **期望动作**：对应的MCP工具调用
- **验证方式**：检查日志或UI反馈

---

## 测试场景

### 一、状态查询类 (Tier 1)

#### 测试 1.1: 询问当前状态
| 项目 | 内容 |
|------|------|
| **用户话语** | "小饮，现在液面怎么样？" 或 "目前的参数是什么？" |
| **期望回复** | "现在选的是奶茶，100ml水、10g粉末、40℃加热中，还没开始冲呢～" |
| **期望动作** | 调用 `self.drink.get_status` → 返回JSON |
| **验证点** | 日志输出 `[uart] 开始注册串口MCP工具...` 中应看到get_status被调用 |

#### 测试 1.2: 询问配方详情
| 项目 | 内容 |
|------|------|
| **用户话语** | "告诉我现在的配方" 或 "这杯饮品怎么配的？" |
| **期望回复** | （与1.1类似的配方信息） |
| **期望动作** | 调用 `self.drink.get_recipe` → 返回当前配置 |
| **验证点** | 日志输出 `[uart] 开始注册串口MCP工具...` |

---

### 二、配方设置类 (Tier 2)

#### 测试 2.1: 简单配方设置（温度调整）
| 项目 | 内容 |
|------|------|
| **用户话语** | "给我冲个热一点的，60度吧" |
| **期望回复** | "好的，改成60℃了，准备冲奶茶～" |
| **期望动作** | 调用 `self.drink.set_recipe` with `{"powder_g": 10, "water_ml": 100, "temp_c": 60}` |
| **验证点** | 日志显示 `[uart] Recipe set: powder=10g water=100ml temp=60C` |

#### 测试 2.2: 完整配方定制
| 项目 | 内容 |
|------|------|
| **用户话语** | "我要150ml水，20克粉末，80度的奶茶" |
| **期望回复** | "收到！150ml水、20g粉末、80℃奶茶，这配置绝了～" |
| **期望动作** | 调用 `self.drink.set_recipe` with `{"powder_g": 20, "water_ml": 150, "temp_c": 80}` |
| **验证点** | UART发送三条指令到STM32：0x13(粉末)、0x12(水)、0x11(温度) |

#### 测试 2.3: 参数越界处理（粉末过多）
| 项目 | 内容 |
|------|------|
| **用户话语** | "给我60克粉末的" |
| **期望回复** | "诶，粉末最多50克啦，我给你来50克的～" |
| **期望动作** | 工具调用失败 → 触发重试或降级处理 |
| **验证点** | 日志显示 `[uart] powder_g out of range: 60` |

#### 测试 2.4: 参数越界处理（温度非标准值）
| 项目 | 内容 |
|------|------|
| **用户话语** | "给我50度的奶茶" |
| **期望回复** | "50度咱们不支持，我给你上40度还是60度？" |
| **期望动作** | 工具调用失败，返回false |
| **验证点** | 日志显示 `[uart] temp_c not in [20,40,60,80]: 50` |

#### 测试 2.5: 水量范围验证
| 项目 | 内容 |
|------|------|
| **用户话语** | "来500ml水的" |
| **期望回复** | "500ml太多了，最多400ml，要来400ml吗？" |
| **期望动作** | 工具调用失败 |
| **验证点** | 日志显示 `[uart] water_ml out of range: 500` |

---

### 三、制作流程类 (Tier 2)

#### 测试 3.1: 开始制作
| 项目 | 内容 |
|------|------|
| **用户话语** | "开始冲吧" 或 "给我来一杯" 或 "开始制作" |
| **期望回复** | "好的，给你冲起来了～" |
| **期望动作** | 调用 `self.drink.start_brew` → UART发送0x20命令到STM32 |
| **验证点** | 日志显示 `[uart] Send start brew command to STM32` + `[uart] Brew started` |
| **UI反馈** | 进度条开始动画，显示"制作中..." |

#### 测试 3.2: 制作中断（取消）
| 项目 | 内容 |
|------|------|
| **用户话语** | "停一下" 或 "稍等" 或 "别冲了" (在制作过程中) |
| **期望回复** | "好的，我停下来了～" |
| **期望动作** | 调用 `self.drink.cancel_brew` → 清空进度条，复位状态 |
| **验证点** | 日志显示 `[uart] Brew cancelled` |
| **UI反馈** | 进度条重置为0%，状态回到配方选择页 |

#### 测试 3.3: 正常完成制作
| 项目 | 内容 |
|------|------|
| **用户话语** | （无需说话，等待制作完成） |
| **期望回复** | "搞定！你的美味饮品已准备好了，请享用～" |
| **期望动作** | STM32 0x82(完成)响应 → OnStm32BrewDone() → 显示完成页 |
| **验证点** | 日志显示 `[uart] OnStm32BrewDone` |
| **UI反馈** | 显示"制作完成"界面，可选择下一杯 |

---

### 四、安全控制类 (Tier 3)

#### 测试 4.1: 紧急停止（制作中）
| 项目 | 内容 |
|------|------|
| **用户话语** | "紧急停止！" 或 "出问题了！" 或 "停！" (紧急语气) |
| **期望回复** | "我已紧急停止，安全第一～" |
| **期望动作** | 调用 `self.drink.emergency_stop` → 强制清空状态 |
| **验证点** | 日志显示 `[uart] EMERGENCY STOP activated!` |
| **UI反馈** | 立即停止进度条，显示"已停止"警告框 |

#### 测试 4.2: 机器错误响应（模拟STM32错误）
| 项目 | 内容 |
|------|------|
| **用户话语** | （无需说话，STM32模拟发送0x81错误码） |
| **期望回复** | "哎呀，机器反馈有问题，我停下来了～" |
| **期望动作** | STM32 0x81 → OnStm32ErrorReport() → 触发cancel_brew |
| **验证点** | 日志显示 `[uart] OnStm32ErrorReport` |
| **UI反馈** | 显示错误提示，恢复到配方选择页 |

---

### 五、综合场景

#### 场景 5.1: 完整工作流（从点单到完成）
```
用户：小饮，来杯奶茶
→ [AI调用 get_status] → AI: "好的，为你准备奶茶～"
→ 用户：加热到60度，200ml水
→ [AI调用 set_recipe(10, 200, 60)] → AI: "配置好了，开始冲吗？"
→ 用户：开始冲
→ [AI调用 start_brew] → AI: "制作中，请稍候～"
→ [STM32返回0x82完成] → AI: "完成！享受你的奶茶吧～"
```

**验证清单：**
- ✅ get_status 被调用1次
- ✅ set_recipe 被调用1次，参数正确
- ✅ start_brew 被调用1次
- ✅ UART发送4条消息（粉末、水、温度、开始）
- ✅ UI进度条完整显示（0→100%）
- ✅ 最终显示完成页面

#### 场景 5.2: 多次调整（用户改主意）
```
用户：给我冲杯奶茶
→ [AI: get_status + start_brew 草案]
→ 用户：等等，我要热的，80度
→ [AI调用 set_recipe(..., 80)] → AI: "改成80℃啦"
→ 用户：现在开始冲吧
→ [AI调用 start_brew] → [正常制作流程]
```

**验证清单：**
- ✅ set_recipe 被调用2次（第一次默认40°，第二次80°）
- ✅ start_brew 只在明确指令后调用1次
- ✅ 无多余的UART命令

---

## 实施建议

### 第一阶段（基础验证）
1. 依次测试 1.1 → 1.2 → 2.1 → 3.1 → 3.2（验证工具链路通畅）

### 第二阶段（边界测试）  
2. 测试 2.3 → 2.4 → 2.5（验证参数验证有效）

### 第三阶段（安全验证）
3. 测试 4.1 → 4.2（验证紧急停止有效）

### 第四阶段（综合验证）
4. 运行场景 5.1 → 5.2（端到端验证）

---

## 日志关键词检查清单

| 工具或动作 | 日志关键词 | 文件 |
|-----------|---------|------|
| get_status | 没有专门日志，检查MCPServer.ParseMessage | application.cc |
| get_recipe | 同上 | application.cc |
| set_recipe | `Recipe set: powder=Xg water=Yml temp=ZC` | MCPTools.cc |
| start_brew | `Send start brew command to STM32` + `Brew started` | MCPTools.cc |
| cancel_brew | `Brew cancelled` | MCPTools.cc |
| emergency_stop | `EMERGENCY STOP activated!` | MCPTools.cc |
| 参数越界 | `powder_g out of range` / `water_ml out of range` / `temp_c not in [...]` | MCPTools.cc |

---

## 常见问题处理

### Q: AI识别到用户意图，但没有调用工具
**A:** 检查系统提示词是否包含工具使用说明，或MCP工具广告是否正确发送（hello消息中`"mcp":true`）

### Q: 工具调用了但没有实际执行UART命令
**A:** 检查MCPTools.cc中的execute_* 方法是否正确调用了machine_send_*_command_回调

### Q: UI显示没有更新
**A:** 检查compact_wifi_board_DKMK.cc的BindDisplayProtocolCallbacks()是否正确绑定

---

## 补充说明

- 所有测试应在**机器模式(machine mode)**下进行
- 建议开启 `idf.py monitor` 监控日志
- UART波特率需确认为 **115200 bps**
- API参数范围（硬编码在set_recipe工具中）：
  - 粉末：5-50克
  - 水：100-400毫升  
  - 温度：20/40/60/80°C
